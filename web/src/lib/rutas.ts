/** URL publica del JSON de la simulacion, tal como la ve el navegador. */
export const URL_SIMULACION = "/data/simulacion.json";

/** Ruta en disco, relativa a la raiz del repo. Espejo de config.RUTA_JSON_RELATIVA. */
export const RUTA_SIMULACION_EN_DISCO = "web/public/data/simulacion.json";

/** Version del esquema que este codigo sabe leer. Espejo de config.VERSION_ESQUEMA. */
export const VERSION_ESQUEMA_SOPORTADA = "1.0";
